/* Set this once, after Render gives you the service URL.
   Leave it empty and the page still works in full on the cached example — it simply
   cannot read a document it has not seen before.

   Example:
     window.KLAUSUL_API = "https://klausul-api.onrender.com";

   ?api=https://… in the address bar overrides this, which is handy for testing. */
window.KLAUSUL_API = "";
